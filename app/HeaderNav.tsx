"use client";
import { useState } from "react";

export default function HeaderNav(){
    // const[auth,setAuth] = useState(false);

    // useEffect(()=>{
    //     onAuthChanged(user=>{
    //         setAuth(!!user);
    //     })
    // },[])

    return <nav>
        {/* {!auth && <Link href="/login">Login</Link>}
        {auth && <Link href="/dashboard">Dashboard</Link>} */}
    </nav>
}